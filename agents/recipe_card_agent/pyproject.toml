[project]
name = "recipe-card-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "gemini-shared",
  "recipe-cards",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/recipe_card_agent"]
