__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/
*.log

.env
.env.*
!dev/.env.local.example
!dev/.env.dev.example

.idea/
.vscode/
*.swp
*.tmp

dev/.env.local
dev/.env.dev
dev/.state/
artifacts/

client_secret*.json
*oauth*client*.json
.oauth/
*.pem
*.key
*.p12
*.pfx
credentials.json
application_default_credentials.json
deployment_result*.json

# Terraform state and plans. State can contain resource detail and must never
# be committed; tfvars can carry environment specifics.
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
*.tfplan
crash.log
crash.*.log
tfplan
provider-schema.json

# Local rendering harness and its output. Rendering a card writes PDFs, PPTXs
# and page images that are reproducible from the agent, so they are scratch
# rather than source.
experiments/
