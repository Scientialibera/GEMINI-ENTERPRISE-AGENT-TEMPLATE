"""Developer environment parsing and Google Cloud project preflight.

Resolves the settings the deployment scripts depend on, and ensures the
project, APIs, staging bucket and each agent's runtime parameter exist.
"""
