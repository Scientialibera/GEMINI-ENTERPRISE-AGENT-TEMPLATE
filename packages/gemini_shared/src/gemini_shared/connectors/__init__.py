"""Clients for Google Cloud services the reference agents call.

Not re-exported from the package root: each module needs a client library that
not every agent ships. Import the module you need directly.
"""
